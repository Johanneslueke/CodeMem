To build this project open a console and go into the build directory!
From there you call:
  cmake ../
to configure your build. The next step is to call
  make
  
You will find the excutable in the build directory.
